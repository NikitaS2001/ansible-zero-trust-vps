---
- name: "VPS Hardening | User | Ensure docker group exists"
  group:
    name: docker
    state: present
  tags: [user]

- name: "VPS Hardening | User | Generate admin password hash"
  set_fact:
    vps_hardening_admin_password_hash: >-
      {{
        admin_password | password_hash(
          'sha512',
          ((admin_user ~ inventory_hostname) | hash('sha256'))[:16]
        )
      }}
  no_log: true
  changed_when: false
  when: admin_password is defined
  tags: [user]

- name: "VPS Hardening | User | Ensure a valid password hash exists (key-only remote mode)"
  set_fact:
    vps_hardening_admin_password_hash: >-
      {{
        admin_password_hash
        if (admin_password_hash | length > 0)
        else (
          lookup('password', '/dev/null chars=ascii_letters,digits')
          | password_hash(
              'sha512',
              ((admin_user ~ inventory_hostname) | hash('sha256'))[:16]
            )
        )
      }}
  no_log: true
  changed_when: false
  when: admin_password is not defined
  tags: [user]

- name: "VPS Hardening | User | Create admin user"
  user:
    name: "{{ admin_user }}"
    groups: "{{ admin_group }},docker"
    # append: no makes group membership convergent: a removed privileged group
    # is dropped on the next run instead of lingering.
    append: no
    state: present
    shell: "{{ admin_shell }}"
    password: "{{ vps_hardening_admin_password_hash }}"
    password_lock: false
    update_password: "{{ 'always' if (admin_password is defined or (admin_password_hash | length > 0)) else 'on_create' }}"
  no_log: true
  tags: [user]

- name: "VPS Hardening | User | Add SSH authorized key"
  ansible.posix.authorized_key:
    user: "{{ admin_user }}"
    key: "{{ vault_admin_ssh_pubkey }}"
    state: present
    # exclusive removes keys that are no longer managed by the role, so key
    # rotation revokes the old key instead of appending it.
    exclusive: "{{ vps_hardening_authorized_keys_exclusive | bool }}"
  # Never run with an empty key: exclusive + a missing pubkey would remove
  # every key from the account and lock the operator out.
  when: vault_admin_ssh_pubkey | default('') | length > 0
  no_log: true
  tags: [user]

- name: "VPS Hardening | User | Configure passwordless sudo"
  copy:
    dest: "/etc/sudoers.d/{{ admin_user }}"
    content: "{{ admin_user }} ALL=(ALL) NOPASSWD: ALL\n"
    validate: 'visudo -cf %s'
    mode: '0440'
  tags: [user]
