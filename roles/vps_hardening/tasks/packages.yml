---
- name: "VPS Hardening | Packages | Update apt cache"
  ansible.builtin.apt:
    update_cache: yes
  tags: [packages]

- name: "VPS Hardening | Packages | Apply package upgrades when enabled"
  ansible.builtin.apt:
    upgrade: "{{ vps_hardening_package_upgrade_mode }}"
  when: vps_hardening_apply_package_upgrade | bool
  tags: [packages]

- name: "VPS Hardening | Packages | Install required packages"
  ansible.builtin.apt:
    name:
      # This provides both sudo and visudo before user.yml creates a sudoers
      # drop-in, including on minimal cloud images that omit sudo.
      - sudo
      - ufw
      - fail2ban
      - unattended-upgrades
      - apt-listchanges
      - curl
      - wget
      - gnupg2
      - ca-certificates
    state: present
  tags: [packages]

- name: "VPS Hardening | Packages | Configure unattended-upgrades (security only)"
  ansible.builtin.copy:
    dest: /etc/apt/apt.conf.d/50unattended-upgrades
    content: |
      Unattended-Upgrade::Allowed-Origins {
          "${distro_id}:${distro_codename}-security";
      };
      Unattended-Upgrade::AutoFixInterruptedDpkg "true";
      Unattended-Upgrade::MinimalSteps "true";
    backup: yes
    mode: '0644'
  tags: [packages]
