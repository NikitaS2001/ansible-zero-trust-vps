[DEFAULT]
bantime  = 3600
findtime = 600
maxretry = 5
ignoreip = {{ fail2ban_ignore_ips | join(' ') }}

[sshd]
enabled  = true
port     = {{ ssh_port }}
logpath  = %(sshd_log)s
backend  = %(sshd_backend)s
